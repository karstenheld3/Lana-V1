```
/critique the specification `_SPEC_GREETER.md`. Write your findings to `_REVIEW_GREETER.md` with one identified finding per flaw (use finding IDs).
```

---

## Step 2: reconcile the findings

```
/reconcile the findings in `_REVIEW_GREETER.md`: mark each finding as accepted or rejected with a one-line rationale, directly in the review file.
```

---

## Step 3: implement the accepted fixes

```
/implement the accepted fixes from `_REVIEW_GREETER.md` in `_SPEC_GREETER.md`. Fix only what was accepted.
```
