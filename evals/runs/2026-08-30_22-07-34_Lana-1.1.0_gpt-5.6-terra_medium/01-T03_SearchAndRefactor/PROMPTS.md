```
Rename the function `calc_total` to `compute_invoice_total` everywhere in this workspace. Find ALL usages first (definition and callers), then apply the rename consistently. Do not change any behavior, formatting, or other names.
```
