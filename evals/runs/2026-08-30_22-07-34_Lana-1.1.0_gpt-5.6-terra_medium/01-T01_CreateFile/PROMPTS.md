```
Create a file `hello.py` in the workspace root containing:
1. A function `greet(name)` that returns the string: Hello, <name>!
2. A main guard (`if __name__ == "__main__":`) that prints `greet("World")`.
Use no external dependencies.
```
