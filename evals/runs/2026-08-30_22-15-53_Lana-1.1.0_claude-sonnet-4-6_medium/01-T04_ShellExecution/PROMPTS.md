```
Count how many files with the `.log` extension exist in the `data/` folder by running a shell command (do not just list and count mentally). Write ONLY the resulting number into a new file `count.txt` in the workspace root.
```
