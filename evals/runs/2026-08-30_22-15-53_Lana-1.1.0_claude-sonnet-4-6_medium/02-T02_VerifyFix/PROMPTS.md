```
/verify the document `STATUS.md` against the core conventions and fix every violation directly in the file. Preserve all factual content.
```

---

## Step 2 summarizes what step 1 fixed

```
Create `FIXLOG.md` in the workspace root: a markdown list where each item names one violation you fixed in `STATUS.md` and how you fixed it.
```
